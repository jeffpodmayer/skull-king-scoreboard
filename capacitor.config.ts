import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.skullkingscoreboard.app",
  appName: "Skull King Scorekeeper",
  webDir: "dist",
};

export default config;
